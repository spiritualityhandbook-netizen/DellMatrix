# 23 — GWS Lens Panel

Status: TRUE

Embeds PersonaLens notes into a GodWorkSpace-style panel.

```python
ui = GWSWithLens()
ui.command("create and bind")
print(ui.render())
ui.collapse_lens()
```

Floor locked. Expand/collapse on `lens` panel.
