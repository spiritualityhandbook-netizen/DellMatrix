# Manual · Flow · Leight · Loure · DDECC (Phase 2 Page 3)

Status: **FORMALIZED**

## Manual // Manuall
Procedure to classify a new term before True write.
1. Run **Mandellafactors**: Has Manor? Has Dell? Has law/rule?
2. Assign category of Mandellistic expression.
3. Clash/test against existing Primaries/variants.
4. Architect number-confirm → True or reject.

## Mandellafactors
Checklist only — not a Dell. Gates growth.

## Leight (Create path ↔ 08)
New Latin-style unit when prefix/root/suffix was undefined. Standalone affix defs required. SUS + Manual + confirm.

## Loure (Change path ↔ 04)
Adopt existing non-Latin affix sense into Latin-Mandel. No silent overwrite of True terms.

## DDECC
**Declare → Define → Execute → Confirm → Correct**
Maps idea → solid form. Aligns with entropy reduction (write forces one shape).

## Flow notation (candidates → True on first runtime pass)
| Op | Role |
|----|------|
| `>` | FlowTo — same chain |
| `>>` | FlowThru — blocking |
| `>>>` | FlowOver — major jump only |
| `:` | FlowBy — elaboration |
| `::` | DeepFlowBy — nested elaboration |
| `:>` | FlowTowards — intent |
| `<:` | FlowFrom — source |
| `<:>` | DynamicFlow — origin·path·target |
| `<<[Delta]` | DeltaFlow — read from checkpoint (anti-forward) |

Rules: Manifest flows into `[]` naturally; Dell needs `:`; capitalize seed English words; `-` for Latin tokens; `_` only if ambiguity requires.

## Mandellacell
Higher-tier cell: multiple seeds bound as one functional unit (above single basic cell).
