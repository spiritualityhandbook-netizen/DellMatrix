# What the program needs — implemented

Assessed gaps that blocked a complete Origin path, now first-class.

## 1. Strong ideas (detail + goals)

```text
create an idea called Code Evolution detail: post-Boolean shells goals: exhaust; honesty
set detail code_evolution deeper text
set goals code_evolution ship loop; stay offline
idea Code Evolution
```

Parsed by `form/idea_create.py` · placed by `form/dell_matrix/needs.py`.

## 2. What next (live NBD)

```text
what next
nbd
```

Ranks next actions from **live state** (nursery pending, weak ideas, pillars, form, enhance).

## 3. Ready checklist

```text
ready
```

Acceptance + health gates. YES only when all checks pass (ideas, strong, grown, sphere/non-cube, pillars, floor, save path).

## 4. Undo + history

```text
undo
history
```

Undo last place or detail/goals edit. History shows note ledger + undo stack.

## 5. Already strong (kept)

- English brain · self-model · inspire · strength suite  
- Avenue ends · garbage guard · workshops/pages  

## Module

`form/dell_matrix/needs.py`

```bash
python3 -m form.dell_matrix.needs
python3 -m form.smoke_all
```
