# src/ — LEGACY (frozen)

**Do not extend.**

This is the old JavaScript dual-era DellMatrix.

Live runtime is **`form/`** only (Python).

See `form/LEGACY.md` and `docs/LIVE_INDEX.md`.

If something here is needed, port it deliberately into `form/` — do not grow `src/`.
