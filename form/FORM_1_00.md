# Form 1.00 — locked product boundary

**Scope statement (required for honest 1.00 scores)**

Form 1.00 is complete when all of the following are true **inside this boundary**:

## In scope (must work)
- Floor locked Alpha · Delta · Omega · Omni
- One program entry: `python -m form.repl` / `python -m form.open` / `python -m form` / boot→open
- Plane: place, skins, perspectives, sandbox, neighbors, zoom page
- Resonance + EnhanceGate (default OFF)
- Main third field + SharedMain disk merge (no personal clobber)
- BlankCube give/import pack
- Persist save/load/checkpoint including enhance + ambient flags
- Visual SVG + interactive HTML
- AmbientGate **skeleton only** (master/sources default OFF; intake returns empty by design)
- NBD equation + SelfGrow curriculum
- Smoke suite + SUS audit record

## Out of scope (not scored; Pre-form)
- File/screen/mic capture engines
- Network Main beyond shared JSON on disk
- Multiplayer realtime simultaneous edit
- Drag-drop 3D GUI editor

Scoring 1.00 means: **nothing left unfinished inside the boundary above.**  
It does **not** mean the Pre-form list is built.
