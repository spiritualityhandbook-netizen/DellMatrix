# form/llm/ — SIDE PACKAGE (not core Origin)

**Not required for Mandell Origin or offline acceptance.**

| Rule | Detail |
|------|--------|
| Scope | Local model bridge experiments |
| Import into open/repl | **Forbidden for core path** |
| Models / network | Optional; Origin must not depend |
| Acceptance path | Must work with this package absent |

See `form/CORE_SCOPE.md`.

Core loop stays offline:

`create → grow → confirm → sphere → save → load → visual`
