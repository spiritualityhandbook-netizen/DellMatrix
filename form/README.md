# FORM — preform gates opened

```bash
python -m form.repl --owner Ace
python -m form.dell_matrix.network_main --serve --port 8765
python -m form.dell_matrix.ambient_gate --smoke
```

Ambient: drop `.txt` into `form/state/{inbox,screen,mic,clipboard}/`  
Sandbox: **default OFF** · `sandbox on|off`  
Network: `set_network` + `net_push` / `net_pull`
