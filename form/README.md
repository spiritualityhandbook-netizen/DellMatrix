# FORM

```bash
python -m form.repl --owner Ace
```

**Sandbox default OFF** (ideas connected).  
`sandbox on` → isolation · `sandbox off` → reconnect all.

```text
matrix> grow ideas 10
matrix> sandbox on
matrix> sandbox off
```
