# Visual Control Language forms — intake

Policy: `form/MODE_FORM.md` (high‑S only).

## Score summary

| Piece | S | Disposition |
|-------|---|-------------|
| Deterministic color/char from syntax | ~0.55 | **In** `visual_terminal.color_for` |
| OpBox + flow links (finite) | ~0.50 | **In** `visual_terminal` |
| Rule 90 fractal + short branch | ~0.48 | **In** optional show |
| Morpheme PREFIX/ROOT/SUFFIX lexicon | ~0.70 | **In** `morpheme.py` expand |
| 300 / 4800 random combinatorial loops | ~0.08 | **Deferred** |
| React CDN HTML host | ~0.05 | **Deferred** (network, not offline Origin) |
| “5000 unique hardware frames” as core | ~0.02 | **Deferred** |

## Use

```bash
python -m form.dell_matrix.visual_terminal --show
```

Optional aesthetics only. Acceptance path stays:

`create → grow → confirm → sphere → save → load → visual`

(HTML offline panel remains the default `visual` command.)
